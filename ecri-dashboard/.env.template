# Copy this file to .env.development.local and fill in your actual values.
# .env.development.local is gitignored and will NOT be committed.

# Gateway API key — must match Gateway:ApiKey in src/AiEnterprise.Gateway/appsettings.Development.json
VITE_GATEWAY_API_KEY=your-gateway-api-key-here
